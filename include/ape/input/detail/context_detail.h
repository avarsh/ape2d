#ifndef APE_CONTEXT_DETAIL_H
#define APE_CONTEXT_DETAIL_H

#include <vector>
#include <unordered_map>

namespace ape {
    namespace input {

        struct Context {

        };

        extern std::vector<Context> contextList;
    }
}

#endif 
